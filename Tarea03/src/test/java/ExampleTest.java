import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;
import java.time.LocalDateTime;

public class ExampleTest {
    /*
     * @Test
     * public void testToStringCitaMedica() {
     * String idPaciente = "12345678";
     * String nombrePaciente = "Juan Arenas";
     * String correoPaciente = "juanarenas@correo.com";
     * String nombreMedico = "Dr. Smith";
     * String especialidad = "Medicina General";
     * LocalDateTime fechaHora = LocalDateTime.of(2024, 1, 1, 10, 0);
     * 
     * Paciente paciente = new Paciente(idPaciente, nombrePaciente, correoPaciente);
     * Medico medico = new Medico(nombreMedico, especialidad);
     * CitaMedica cita = new CitaMedica(paciente, medico, fechaHora);
     * 
     * String esperado = "Cita con " + nombreMedico + " - " + especialidad +
     * " para " + nombrePaciente + " ("
     * + idPaciente + ") en " + fechaHora + " [Agendada]";
     * assertEquals(esperado, cita.toString());
     * }
     * 
     * @Test
     * public void testSetEstadoCitaMedica() {
     * String idPaciente = "12345678";
     * String nombrePaciente = "Juan Arenas";
     * String correoPaciente = "juanarenas@correo.com";
     * String nombreMedico = "Dr. Smith";
     * String especialidad = "Medicina General";
     * LocalDateTime fechaHora = LocalDateTime.of(2024, 1, 1, 10, 0);
     * 
     * Paciente paciente = new Paciente(idPaciente, nombrePaciente, correoPaciente);
     * Medico medico = new Medico(nombreMedico, especialidad);
     * CitaMedica cita = new CitaMedica(paciente, medico, fechaHora);
     * cita.setEstado("Cancelada");
     * assertEquals("Cancelada", cita.getEstado());
     * }
     */
}
